[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "memnos-gateway"
version = "0.1.0"
description = "memnos mobile gateway — Telegram and WhatsApp"
requires-python = ">=3.11"
dependencies = [
    "memnos-core",
    "memnos-orchestrator",
    "python-telegram-bot>=21.0",
    "httpx>=0.27",
    "fastapi>=0.111",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["memnos_gateway*"]
